name: Setup
description: Set up Go, with optional Linux OpenGL/X11/Wayland headers and the fyne CLI.

inputs:
  gl:
    description: Install Linux OpenGL/X11/Wayland headers (needed for CGO desktop builds).
    default: 'false'
  fyne:
    description: Install the fyne CLI.
    default: 'false'

runs:
  using: composite
  steps:
    - uses: actions/setup-go@v5
      with:
        go-version: '1.26'

    - if: inputs.gl == 'true' && runner.os == 'Linux'
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends \
          gcc \
          libgl1-mesa-dev \
          xorg-dev \
          libwayland-dev \
          libxkbcommon-dev

    - if: inputs.fyne == 'true'
      shell: bash
      run: go install fyne.io/tools/cmd/fyne@latest
